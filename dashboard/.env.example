# Cloudflare Worker API endpoint
VITE_API_URL=http://localhost:8787

# API Key for Worker authentication
# For local dev: optional (if worker runs without auth check)
# For production: REQUIRED (must be in D1 whitelist)
# Generate with: openssl rand -hex 32
VITE_API_KEY=your-generated-api-key-here

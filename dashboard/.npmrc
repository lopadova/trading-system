# Dashboard npm configuration
# Prefer npm for test commands (Bun vitest doesn't support DOM environments)

# If you run 'bun test', it will use Bun's vitest which doesn't support jsdom/happy-dom
# Use 'npm test' or 'bun run test' instead

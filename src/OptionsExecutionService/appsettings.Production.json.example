{
  "__": [
    "OptionsExecutionService — PRODUCTION overrides (Phase 7.5).",
    "Copy to appsettings.Production.json (gitignored), replace placeholders,",
    "and wrap ALL sensitive values via EncryptConfigValue. See",
    "docs/ops/SECRETS.md § Provisioning.",
    "",
    "TradingMode STAYS \"paper\" — flipping to \"live\" requires an explicit",
    "operator decision AND the confirmation step in install-options-engine.ps1."
  ],
  "TradingMode": "paper",
  "Cloudflare": {
    "WorkerUrl": "https://ts.padosoft.workers.dev",
    "ApiKey": "DPAPI:REPLACE_WITH_PROTECTED_BASE64"
  },
  "Smtp": {
    "Enabled": true,
    "Host": "smtp.gmail.com",
    "Port": 587,
    "Username": "REPLACE_WITH_SMTP_USER",
    "Password": "DPAPI:REPLACE_WITH_PROTECTED_BASE64",
    "From": "REPLACE_WITH_FROM_ADDRESS",
    "To": "REPLACE_WITH_TO_ADDRESS",
    "EnableSsl": true
  }
}
